# Reads two integers representing the dimensions (rows and columns) of a matrix. This foundational step prepares for subsequent matrix input operations.

rows = int(input())
columns = int(input())

print(rows, columns)

# Reads a decimal number and formats it to exactly two decimal places using the .format() method. Demonstrates precision control in numeric formatting.

number = float(input())

print("{:.2f}".format(number))
# Reads extremely large integers (up to 10^18) as input. The program handles these large values without precision loss, often treating them as strings or using appropriate numeric types.

large_number = int(input())

print(large_number)
# Reads a decimal number and formats it to exactly two decimal places using the %-formatting operator. Shows traditional precision formatting approach.

number = float(input())

print("%.2f" % number)